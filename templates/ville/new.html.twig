{% extends 'base.html.twig' %}

{% block title %}Ville{% endblock %}

{% block body %}
   <div class="container">
    <h1 class="h1Twig">Créer une nouvelle ville</h1>

    {{ include('ville/_form.html.twig') }}

    <a href="{{ path('app_ville_index') }}" class="btn btn-primary m-1">Retour à la liste</a>
   </div>
{% endblock %}
