{% extends 'base.html.twig' %}

{% block title %}Ville{% endblock %}

{% block body %}
   <div class="container">
    <h1 class="h1Twig">Éditer la ville</h1>

    {{ include('ville/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('app_ville_index') }}" class="btn btn-primary m-1">Retour à la liste</a>

    {{ include('ville/_delete_form.html.twig') }}
   </div>
{% endblock %}
