<form method="post" action="{{ path('app_sous_categorie_delete', {'id': sous_categorie.id}) }}" onsubmit="return confirm('Vous voulez supprimer cette sous catégorie?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sous_categorie.id) }}">
    <button class="btn btn-danger m-1">Effacer</button>
</form>
