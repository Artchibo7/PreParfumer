{% extends 'base.html.twig' %}

{% block title %}Sous Catégorie
{% endblock %}

{% block body %}
	<div class="container">

		<h1 class="h1Twig">Sous-catégorie</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ sous_categorie.id }}</td>
				</tr>
				<tr>
					<th>Nom</th>
					<td>{{ sous_categorie.Nom }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('app_sous_categorie_index') }}" class="btn btn-primary m-1">Retour à la liste</a>

		<a href="{{ path('app_sous_categorie_edit', {'id': sous_categorie.id}) }}" class="btn btn-success m-1">Éditer</a>

		{{ include('sous_categorie/_delete_form.html.twig') }}
	</div>
{% endblock %}
