{{ form_start(form) }}
    <label class="text-info">Nom de la sous-catégorie</label>
    {{ form_widget(form.Nom, {'attr': {'class':'form form-control'}}) }}
    <label class="text-info">La catégorie</label>
    {{ form_widget(form.Categorie, {'attr': {'class':'form form-control'}}) }}
    <button class="btn btn-info m-1">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
