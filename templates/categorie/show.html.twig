{% extends 'base.html.twig' %}

{% block title %}Catégorie
{% endblock %}

{% block body %}
	<div class="container"><br><br>

		<h1 class="h1Twig">Catégorie</h1><br><br>

		<table class="table">
			<tbody class="table-group-divider">
				<tr>
					<th>Nom</th>
					<td>{{ categorie.Nom }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('app_categorie_index') }}" class="btn btn-primary m-1">Retour à la liste</a>

		<a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}" class="btn btn-success m-1">Éditer</a>

		{{ include('categorie/_delete_form.html.twig') }}
	</div>
{% endblock %}
