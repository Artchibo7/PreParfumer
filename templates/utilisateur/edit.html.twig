{% extends 'base.html.twig' %}

{% block title %}Éditer Utilisateur
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="h1Twig">Éditer l'utilisateur</h1>

		{{ include('utilisateur/_form.html.twig', {'button_label': 'Update'}) }}

		<a href="{{ path('app_utilisateur_index') }}" class="btn btn-primary m-1">Retour à la liste</a>

		{{ include('utilisateur/_delete_form.html.twig') }}
	</div>
{% endblock %}
