{% extends 'base.html.twig' %}

{% block title %}Éditer Produit
{% endblock %}

{% block body %}
	<div class="container"><br><br>
	
		<h1 class="h1Twig">Éditer le produit</h1><br><br>

		{{ include('produit/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

		<a href="{{ path('app_produit_index') }}" class="btn btn-primary m-1">Retour à la liste</a>

		{{ include('produit/_delete_form.html.twig') }}
	</div>
{% endblock %}
